databases:
  - name: cotizador-casa-diseno-v2-db
    plan: free
    databaseName: cotizador_casa_diseno
    user: cotizador

services:
  - type: web
    name: cotizador-casa-diseno-v2
    runtime: node
    plan: free
    buildCommand: npm ci --include=dev && npm run test && npm run build
    startCommand: npm start
    healthCheckPath: /api/health
    autoDeployTrigger: commit
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: cotizador-casa-diseno-v2-db
          property: connectionString
      - key: NOTIFICATION_TO_EMAIL
        value: contacto@cdchile.cl
      - key: RESEND_API_KEY
        sync: false
      - key: NOTIFICATION_FROM_EMAIL
        sync: false
